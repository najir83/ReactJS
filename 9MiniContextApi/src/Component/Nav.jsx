import React from 'react';
import Button from './Button';
const Nav = ({count}) => {
    return (
        <div>
            nav
            <Button count={count}/>
        </div>
    );
}

export default Nav;
